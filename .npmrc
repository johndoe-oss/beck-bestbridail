# npm equivalent of the project's original pnpm settings (auto-install-peers=false,
# strict-peer-dependencies=false) — don't hard-fail installs over peer dependency
# mismatches (e.g. esbuild-plugin-pino's peer range vs. the pinned esbuild@0.27.3).
legacy-peer-deps=true
